# 「文档里声明了、但源码里还没有」的白名单。
#
# 格式：标识符  预定版本  说明
#
# ⚠️ 这不是例外表，是【带到期日的欠条】：
#
#   预定版本一旦打过 tag，doccheck 直接 FAIL。
#
# 不靠人自觉去清理，靠时间推着清。否则白名单迟早变成垃圾桶——
# 和「一个会红的必过项迟早被加 || true」是同一个形状。
#
# 加一项之前先问：它是真的排在后面，还是我不想现在处理它？
#
# ---- ⚠️ 白名单的代价：这张表里的类型，它们的【字段】不受守卫 ----
#
# doccheck 的字段比对只在类型【已实现】时才触发（源码里找不到就走白名单分支）。
# 于是白名单里的类型，其字段从不被拿去和源码比——**它们本来也没有源码可比**。
#
# 代价是真实的：v0.3 设计里 `SyncRequest.From/To`、v0.6 的 `RollRule.Pick`、
# `Roll.Day` 三处都把 `TradingDay` 写成了 `int32`——**和 doccheck 在
# `Bar.TradingDay` 上抓到的是同一族错误**，而它们在这里活了下来，
# 因为守卫够不到还没实现的那一半。
#
# ⇒ **「还没实现」的那部分文档，是这张表用覆盖面换来的盲区。**
#    实现它的那天，字段比对第一次生效——那时冒出来的分歧不是新引入的，
#    是一直在那里、只是没人看得见。**别把它当成回归。**
#
# ---- 打 tag 那天，这张表【不该】红 ----
#
# 红了就是有一步没做，两种，doccheck 分开报：
#
#   「白名单已过期」    预定版本打了 tag，而源码里还是没有 → 实现没做完
#   「欠条已兑现，没清」 源码里已经有了 → 实现做完了，这一行忘了删
#
# 所以【不要】提前给这个红发通行证。说「打那一档的 tag 那天它们会一起到期、
# 这是设计如此」是错的——一个被预告过会红的检查，和一个被加了 `|| true`
# 的检查，最终下场一样：没人再读它。
#
# ⚠️ 这句话原本写的是一个具体的版本号。2026-09-08 里程碑整体后移一格时把它拿掉了，
#    理由不是「改起来麻烦」，是：**一个写在散文里的版本号，是一份没人守的第二拷贝**。
#    跟着改一次，下一次里程碑变动它会再坏一次；改成指代，它就不用再跟着走。
#
# ---- 打 tag 之前先空跑一次 ----
#
#   git tag v0.X.0 HEAD && go run ./tools/doccheck; git tag -d v0.X.0
#
# 它会当场告诉你这张表清干净了没有。
# 通则：**一个「条件成立时才失败」的检查，要在条件成立之前先模拟一次条件**——
# 否则暴露时刻就是你已经没得选的那一刻（v0.0.0 那次「批的 commit ≠ 打 tag 的
# commit」就是在打完之后才暴露的）。

# ---- v0.3：拉取 / 存储 / 同步 ----
SyncRequest   v0.3.0  同步请求；Force 用于重拉被固化成「确认无数据」的抖动
Syncer.Sync   v0.3.0  按交易日记 coverage，只拉缺失区间
SyncReport    v0.3.0  带「请求区间 vs 日历覆盖区间」两行；Misaligned 扫 BarBound.Anomalous
NightAbsent   v0.3.0  连续无夜盘那一段（Days/From/To）；只报不判 —— 实测分布 1×52 段 / 64×1 段，中间空，见 probe.md 6.9
AggClockGrid      v0.5.0  AggRule 的成员：时钟网格
AggTradingAxis    v0.5.0  AggRule 的成员：交易时间轴 + 相位
NoAdjust          v0.6.0  AdjustMethod 的成员：不复权
RatioBack         v0.6.0  AdjustMethod 的成员：后复权·比例
RatioFwd          v0.6.0  AdjustMethod 的成员：前复权·比例
DiffBack          v0.6.0  AdjustMethod 的成员：后复权·差值
DiffFwd           v0.6.0  AdjustMethod 的成员：前复权·差值

# ---- v0.5：深度分钟历史落地后，聚合口径才有意义 ----
AggRule       v0.5.0  时钟网格 / 交易时间轴两套；**默认取哪套未验，不设默认**

# ---- v0.6：主力连续 ----
ContinuousSpec v0.6.0  换月规则 + 复权方式
RollRule       v0.6.0  换月规则接口
RollRule.Pick  v0.6.0  在候选合约里选主力
AdjustMethod   v0.6.0  五种复权方式；默认后复权，前复权会改写历史
Roll           v0.6.0  一个换月点：日期 / 新旧合约 / 基差 / 复权因子
Continuous     v0.6.0  拼好的序列 + 接缝（Rolls 必须交出去）
