# 「文档里声明了、但源码里还没有」的白名单。
#
# 格式：标识符  预定版本  说明
#
# ⚠️ 这不是例外表，是【带到期日的欠条】：
#
#   预定版本一旦打过 tag，doccheck 直接 FAIL。
#
# 不靠人自觉去清理，靠时间推着清。否则白名单迟早变成垃圾桶——
# 和「一个会红的必过项迟早被加 || true」是同一个形状。
#
# 加一项之前先问：它是真的排在后面，还是我不想现在处理它？

# ---- v0.2：拉取 / 存储 / 同步 ----
Source        v0.2.0  Source 接口
Source.Bars   v0.2.0  拉 K 线
Source.Caps   v0.2.0  能力查询；深度按 (ProductKey, Period) 给，不能只按 Period
Capabilities  v0.2.0  各源能力差异做成数据而不是散落在文档里
SyncRequest   v0.2.0  同步请求；Force 用于重拉被固化成「确认无数据」的抖动
Syncer.Sync   v0.2.0  按交易日记 coverage，只拉缺失区间

# ---- v0.4：深度分钟历史落地后，聚合口径才有意义 ----
AggRule       v0.4.0  时钟网格 / 交易时间轴两套；**默认取哪套未验，不设默认**

# ---- v0.5：主力连续 ----
ContinuousSpec v0.5.0  换月规则 + 复权方式
RollRule       v0.5.0  换月规则接口
RollRule.Pick  v0.5.0  在候选合约里选主力
AdjustMethod   v0.5.0  五种复权方式；默认后复权，前复权会改写历史
Roll           v0.5.0  一个换月点：日期 / 新旧合约 / 基差 / 复权因子
Continuous     v0.5.0  拼好的序列 + 接缝（Rolls 必须交出去）
