# 送审、放行、合并

本仓有一道**外部评审闸门**：合并到 `main` 并打 tag 前必须先过评审。
下面只写**送审方**要做的动作。评审方怎么审是他那边的事——
**他的独立性正是他的价值**，本仓不替他规定。

---

## 一、送审要给下面这些，缺一退回

1. **版本号 + 对应的排期行**（`README.md` 最后那张表）；
2. **`git log --oneline main..HEAD`** + 变更文件清单；
3. **自测的真实命令与真实输出**；
4. **`git diff --stat <base>..HEAD`**；
5. **改载体之后那次「先红」的输出**——见下面「先红再重造」；
6. **本版若不会红，要举证【为什么不会红】**——见下面「不会红的也要举证」；
7. **每一次用来下结论的测量，前后两次 `git status --porcelain` 的输出**——
   两次都必须为空，**空输出也要贴**（见下面「测量的环境也要举证」）。

第 4 条是评审方 2026-09-08 提的，起因是我把两个构建中间产物（`*.gen`）
一次宽 `git add` 扫进了送审版本，**而送审信里一个字都没提**。

> **它把「这一版做了几件事」变成一个【要看的东西】，而不是一个【要记得问的问题】。**
> 「一版只做一件事」靠自律是守不住的——失守的方式往往是无意的。

⚠️ 而它有一个前提：**`<base>` 必须是当前的 `main`**。分支落后于 `main` 时，
这张表会把「main 里有而我这儿没有」显示成**我删掉了它们**——
那时它暴露的不是范围，是假象。**贴之前先确认分支不落后，或者先 rebase。**

### 第 7 条：**测量的环境也要举证**

一次测量的结论可不可信，有**两个轴**，而只有第一个平时被看：

```
轴一  退出码怎么解释      —— 见 docs/design.md §14.2 ①（那是一张会长的表）
轴二  这次测量在什么环境里做的 —— 树干净吗？有没有别的东西正在改它？
```

⚠️ **轴二没有任何现成信号**：`go test` 不知道工作区脏，而它照样会给出一个
看起来完全正常的红或绿。**2026-09-08 两次都栽在这里**：

```
实例一  一个会变异载体的后台作业与「合并前重跑全套」并发
        ⇒ 产出一个【红】 ⇒ 被忽略 ⇒ 侥幸无害
实例二  上一次运行被 timeout 杀掉、留下一行没还原，下一次从那棵树上开跑
        ⇒ 产出一个【满分】 ⇒ 而满分不会促使任何人去查仪器
```

> **实例二危险得多**：**不利的结果会让你怀疑仪器，有利的不会。**
> 一个用来测「守卫会不会响」的实验，跑在一棵本来就会红的树上，**会给出满分**——
> 和「用生成器自己的模式测生成器」是同一条，**只是这次的载体是【环境】而不是【方法】。**

⇒ 所以贴那两次输出，**而不是加一条「记得跑 `git status`」**：
**要记得做的动作会失效，要提交的产物不会。** 判据与例外见
[`docs/design.md`](docs/design.md) §14.2 ②''。

### **会变异工作区的测量，去扔掉的克隆里跑**

理由不是「安全」：

> **它把「测量期间不许碰这棵树」这条【要记得的纪律】，变成一个【结构性事实】**——
> 克隆里怎么跑都碰不到主树。**而这一路已经反复确认：要记得的纪律会失效。**

⇒ 它同时把**轴二的两半**都关掉：并发那一半（改不到主树）和残留那一半（克隆随时可重置）。

**必须一起报的**：

```
克隆的 SHA ＋ 它开跑前的 git status --porcelain
```

**不报这两样，「哪棵树」这个问题就会换个形式回来**——
下一个人无从知道那次结果是在什么东西上量出来的。

### 第 5 条：**改载体之后，先跑一次测试看红，再重造**

顺序反了，那一次红就**永远不存在**。

> **先重造再跑，等于只验了「改完之后是绿的」，没验「改之前是红的」。**
> **一个永远不红的守卫，和一个永远绿的守卫，在那种跑法下长得一样。**

⇒ 送审信里贴那次红的输出。**把一个要记得做的动作，变成一个要提交的产物**——
和第 4 条（贴 `git diff --stat`）是同一类。

### 第 6 条：**不会红的也要举证**，否则「本版不会红」是一句不用证明的话

有些版本**本来就不会红**：比如只在一个已有的加粗行【内部】改字——
表格行不算规矩，普查数的是含 `**` 的**行数**，行数没变 ⇒ 三张表一处也看不见。

⚠️ **而「本版不会红」和「我忘了先跑」在送审信里长得一样。**

```
会红的    贴那次红的输出
不会红的  贴【为什么不会红】+ 一次对照实验
```

**第二条才是关键**：它把「不会红」从一个**声明**变成一个**有产物的动作**。
（实例：那次行内改字，对照实验是「把整行删掉 → 普查当场红，登记 161 实际 160」——
**证明了「看不见这次编辑」不等于「根本没检查」。**）

第 3 条是字面意思：**不接受「测试通过」四个字**。

> **一条被丢弃了输出的命令，不能拿来当「已完成」的依据。**
> 要么留输出，要么事后用一条**独立**命令查状态。

（本仓两次栽在这里：patch 脚本 `replace` 不匹配时静默返回原串、
然后无条件 `print("patched")`——commit message 说改了而代码逐字未动；
`git branch -d … >/dev/null 2>&1 && git push --delete` ——错误被吞、
`&&` 让第二步根本没跑、报告写了「分支已清理」。**同一个形状。**）

#### 同一条的另一半：**用 `;` 串起来的流水线，前一步失败不会拦住后一步**

真事（2026-09-08）：重建 `docs_test.go` 的三张表是三个脚本用 `;` 和 `>/dev/null` 串的。
**截断那一步报 `substring not found`，而后面几步照跑**——往一个残缺的文件上又接了两张表，
连 `package` 都没了。

> **「后面几步跑完了」不等于「整条跑成了」。**
> 一条流水线要么 `set -e`，要么写成一个脚本，**每步断言、失败即整份还原**。

修法落在 `tools/audit/rebuild_docs_test.py`：动手前整份备份、每步断言、
写完跑 `gofmt` + `go vet`，**任何一步失败就把原文件逐字节写回去，盘上不留半成品**。

#### ⚠️ 而同一天，这条规矩**在隔壁那个脚本上一处都没有**

评审方 2026-09-08 实测 `render_escape_check.py`：把载体改坏再跑它，
它**正确地打印**了「还原没做干净」，然后**退出码 0**。

> **一条规矩在 A 文件立住、在 B 文件失效，比没立更值得一提。**

#### 而**降级一条意见的理由，和提出它的理由一样要量**

同一轮里评审方给 R4 降级，理由是「挪动在 diff 里可见，而改写不可见」。
**改写当然可见。** 而他自己复盘出来的更难看：
**挪动比改写更不可见**（diff 里同一段文字出现两次，扫内容什么新东西都看不到），
**他却拿这条理由去给更隐蔽的那个降级**。

> **一个降级理由如果成立，会把你正在评审的这整套东西一起否掉，
> 那它就不是降级理由，是一个自毁的论证。**
> 拿已经承认的那条（R1 改写）试一次，两秒就能发现。

⇒ **提要求是廉价的，给不阻塞的理由才说明这一格真的量过**——
所以那个理由本身也要有依据。
> （它是第二十九条「一条刚学会的规则默认只作用在学它的那个位置上」的
> 最刺眼的形态：**两个位置是同一个人在同一天写的**。）

我按这条把 `tools/audit/` 下**全部**脚本过了一遍，**中的不是一个，是四个**：

```
render_escape_check.py    对照组只 print，退出码不受影响
section_move_check.py     同上（我从前者复制来的）
sweep_carrier_rules.py    找到漏洞 / 对照组失败 / 还原不干净，一律退 0
                          ——而我一直在把它的「0 漏、对照组 3/3」当证据引用
gfex_offset_recheck.py    取窗失败只 print「不报计数」，然后继续
```

> **「一条被丢弃了输出的命令不能当依据」的镜像是：
> 一条【退出码没有意义】的命令，同样不能放进任何链子里。**

四个都修了，且**两个方向各验一次**（正常 → 0；把前提弄坏 → 1）。

> ⚠️ **备份要验，否则备份本身是第三种静默失败**（评审方 2026-09-08）。
> 「失败就逐字节还原」这条路径**写下来了，但从没被执行过**——
> **一条没被执行过的路径，和没有这条路径的区别只在读者的印象里。**

所以脚本带自我演练：`python tools/audit/rebuild_docs_test.py --drill`
——它走遍 `restore()` **入参的取值空间**（`bytes` 与 `None` 两种形状），
**每一支都在输出里写明走的是哪一条**：

```
取值一 original=bytes：写坏 55 字节 → 把 docs_test.go 逐字节还原了 → 逐字节相同
取值二 original=None ：生成器写了一份 → 把 docs_test.go 删掉了（它本来就不存在）→ 盘上确实没有它
```

**另外真让主流程失败过一次**（喂一节没有锚点候选的小节），`docs_test.go` 的 md5 未变；
后来在**真事故**上又走过两次（补丁脚本的反斜杠穿层），两次都当场拦住并逐字节还原。

#### ⚠️ 这一段原来写的是「带**一次**自我演练」，而演练早已是两支

那句话在演练变成两支之后**只描述了一半，而它一个字都没改**。起因值得单记：

> **一条新的验收判据，会把代码推进一个它从来没进过的状态。**
> 「删掉能不能一模一样地造回来」要求先 `rm` ⇒ `original` 可能是 `None`；
> 主流程为此加了那个分支，**而演练到不了那儿**（它第一行就无条件 `open()`）。

⇒ **给主流程加一个新的失败形态时，问：演练走得到那儿吗？**
走不到 ⇒ **它认证的射程缩了，而它的输出一个字没变。**

处置不是补那一个洞，是让「演练与主流程漂移」在结构上不成立：
**还原只有一份实现，演练和主流程调的是同一个**——
上一版 `drill()` 是把主流程那一行**抄**了一份，注释还写着「这就是失败分支做的那一件事」，
而**一段注释说「这就是那一件事」，就是在承认这里有第二份拷贝。**

### 材料里说「我做不到 / 分不开 / 想不出」的地方，**必须紧跟一句报价**

> **凡出现「分不开 / 想不出 / 做不到 / 无法确定」，必须紧跟【再做一次要多少钱】。
> 给不出成本的，那不是一条限制，是一个没做的检查。**

因为这类句子**穿着克制的外衣**：「我分不开，所以我不下结论」读起来像严谨，
于是它**同时骗过写的人和审的人**。真事三连，成本分别是 172 秒、一次 Range、一次乘法：

```
「我想不出怎么触发它」        → 把【缺口】说成【结论】
「我的数据分不开这两种解释」  → 把【没做的检查】说成【认识论限制】
「0.05% 搬不动 25% 的内容」   → 把【没做的算术】说成【不可能】
「contract.md 里还有一句」    → 把【没做的 grep】说成【一个事实】
```

⚠️ 第四句是**肯定式**的，所以那条通则不能只写否定式：

> **不是「把『我没做 X』说成『X 不成立』」，是说成【任何一个关于 X 的断言】。
> 肯定式的更危险，因为它读起来像信息，不像判断。**

#### ⚠️ 这条自检自己的射程：**它是关键词触发的**

「目前没有办法区分」「缺乏依据」「尚不清楚」「暂时无解」——**换个说法就绕过去了**。
按本仓那条「一次审计的完整性受限于它用的模式」，**这条自检同样受限于它的词表**。

**写下这一句是必要的**：不写，它就会变成又一个「看起来那一格有人守」的东西。
它挡的是**会传到第二个人手里的那些**——而第二个人是唯一可能拦住它的人。

## 二、每封报告状态的消息

不带锚点的状态消息会造成「消息交叉」——一封在途中就被下一封推翻，
双方都在拿「我发出时的状态」回应「对方发出时的状态」。

**把「哪一刻的快照」写进快照本身。**

顺带：`remotes/origin/*` **不是**远端状态，是「上次 `fetch` 时的远端状态」。
问当下用 `git ls-remote`。

## 三、送审之后往同一分支加东西

可接受的条件是**两条同时成立**：

1. **主动报**；
2. **评审方能【机械地】验证新增部分与送审对象不相交**。

接受的理由不是「改动小」，也不是「报了」，是**可验证**；
主动报只是让验证变便宜。**让 ② 变便宜是送审方的责任**——
把不相关的工作放进只碰不相交路径的独立提交。

⚠️ 「按评审意见修改送审对象」**不属于**这一条，那是有条件通过的应有动作。

#### 而这两条之外还有一个**前提**：评审进行中时，**靶子不动**

> **评审对象必须是个不动的靶子。**
> 你满足了上面两条（主动报 + 可机械验证不相交），仍然可能让评审方
> **批的 SHA 不等于当时的 tip**——那时他批的是一个已经不存在的状态。

真事：同一轮里连着发生两次，两次都是送审方一边等评审一边往分支上加。
**规则的字面满足了，目的没满足。**

⇒ 送审之后到收到结论之间，**不再往这条分支加东西**；
确实发现了缺陷（比如你自己查出送审对象里有错），**改可以，但要主动报

#### ⚠️ 而「**他还没开始评**」不能当理由——**那个事实你不掌握**

真事（2026-09-08）：我破冻结时给的理由是「这一版评审方还没开始看」。
**他已经评完了，并且判了不通过**——我们只是消息交叉。

> **「评审方还没开始看」不是送审方能确立的事实，因此它不能用来支持任何自我豁免。**
> 冻结之后要动，**唯一安全的做法是问一句**——那个前提只有对面知道。

这比「目的不能由送审方单方面解释成豁免」更具体一层：

#### 冻结的正确适用范围：**从「合并」扩到「提交」**

本轮双方连着交叉了六次，每次都是「他评 N，我在 N+1」。一次代价具体是：
**评审方花一整轮全套重跑，去测一个在下一版里已经不存在的洞。**

> **这不是新规矩，是冻结那条的正确适用范围**（评审方 2026-09-08）：
> 冻的本来就是「评审期间分支不动」，而交叉的成因恰恰是**评审期间分支在动**。

⇒ **送审之后到收到结论之间，新发现只写进消息，不写进分支。**

#### 而「我在下一封里列出来给你对」这个兜底**不成立**

我原本是这么提的。评审方一句话驳掉：**「我拿什么对？对你的记忆。」**
——那正是「消息里的松」，**等于用那一格去守那一格**。

> **换成：评审方在结论信里，把收到的那批新发现【原样回列一遍】。**
> 于是两边各有一份**独立产出**的清单，下一版拿送审方的落地清单对
> **评审方的回列**，不是对送审方自己的记忆。

这就是 `docs_test.go` 那次用过的手法：
**凡「两处必须一致」，让它们各自产出可比较的东西，别让人去读两边的实现。**

#### ⚠️ 例外，必须有：**发现的是【正在评审的这一版】的缺陷 ⇒ 立刻发，不许攒**

攒的只能是改进和新守卫。

> **否则评审方会批准一个【送审方已经知道是坏的】版本——那比交叉严重得多。**
**不是「你的解释不算数」，是【你用来解释的那个事实，你不掌握】。**
「我破了冻结，为什么」**——因为「目的不能由送审方单方面解释成豁免」。
这一条专指**与评审无关的新增**。

## 四、放行清单：**每一项标明「已批准」还是「需另行送审」**

> **「我建议你做 X」不等于「X 已放行」——建议是输入，批准是输出，清单上必须分列。**

放行方要把「从此刻到打 tag 之间预期发生的每一个改动」逐条列出来；
**送审方打算做而清单上没有的，动手【之前】问，不是做完再报。**

> **「事后主动说」和「事前确认」不是一回事——前者让错误可纠正，后者让它不发生。**

（本仓两次「批准的 commit ≠ 打 tag 的 commit」都出在这里。第二次的原因不是有人违规，
是**放行那句话不够具体**：那两行改动正是评审方两轮前自己要求过的，
却没写进放行清单——而后来的人**只看得到放行那句话**。）

### 发现「批准的 SHA ≠ 要合并的 SHA」时，**停下来问**

不是加一行注解继续。

> 我在合并那一刻就知道 SHA 不同——**我把它打印出来了，然后我合并了。**
> **把疑虑打印出来，不等于解决了疑虑。**
> 我为它写了一行解释，**说明我心里知道它需要解释——需要解释的合并，就是需要问一句的合并。**

⇒ 可观察信号：

**你正在为一个动作写解释，这件事本身就是那个动作需要被批准的证据。**

### 一个批准在**条件变了之后**还算不算数：先分清你在干什么

> **判据是——你是在【援引】一个批准，还是在【丢弃】它？**
>
> - **援引** ⇒ 要问它的前提还在不在。
> - **丢弃** ⇒ 不需要请示，**但要说明丢了什么。**

这条把「批准依附于它当时的机制」变得可操作。**那句话单独会读错**——
读成「机制一变就得重新请示」，于是每一处无关的改动都要跑一趟放行方。

#### 「丢弃」为什么可以不请示，而又为什么仍要说

> **不需请示，是因为它不借用对方的判断力**；
> **仍要说明，是因为对方可能正拿着那个批准在别处做推理。**

⇒ **说明的对象不是那个决定本身，是「那个批准从此不再有效」这件事。**

（实例：一个阈值 `0.9` 被批准过，后来那个阈值整个去掉了。
去掉它是**丢弃**——没有借用当初那句批准的任何判断，所以不必问；
但要说一声，因为放行方手上那条「`0.9` 已批」可能正被他用在别的推断里。）

⚠️ **最容易错的是把「援引」误当成「丢弃」**：
留着那个结论、只换掉它底下的机制，然后因为「反正我没请示、我是丢弃」而不问。
**那是援引**——你仍在花那句批准的信用。

## 五、并行分支：**同一文件上都有改动，就串成线**

**分别评 A、分别评 B，不等于评了 A+B。** 合并后的那份文件是**第三个产物**，
没有任何一次评审看过。「合得干净」是**机械**性质，「合完还是对的」是**语义**性质，
前者推不出后者。

⇒ 让每次评审对象 **== 那次合并的结果**：先评先并 A，B 再 rebase 到新 `main` 上评。

#### ⚠️ 顺带一格：**新加的守卫【不】覆盖已经在飞的分支**

评审方 2026-09-08 注意到的：`design/v02-storage-format` 是从孤句守卫**出现之前**
的提交开出来的，所以那 109 行新散文**从来没被孤句守卫看过**——
**rebase 才是它们第一次碰面。**

> **守卫是按【树】跑的，不是按【改动】跑的。** 一个分支开出去之后，
> 主干上新加的守卫对它不生效，直到它回到主干。

⇒ 所以合并一条**开得早、活得久**的分支时，rebase 之后要**重跑全套**，
而不是复用 rebase 之前那次的结果——**那次跑的是一棵没有新守卫的树。**

### 生成物冲突时**不合，重造**——但先过判据这一关

两条分支都改了同一个**生成**出来的文件 ⇒ **别去合它。**
取任一侧，再**重跑生成器**，用它的输出覆盖。

> **合并算法看的是行，而生成物的意义不在行里。**
> 逐行合出来的那一份，两边的输入都没生成过它——**它是一份谁都没生成过的生成物。**

#### 🔴 而这条规矩的**理由层**，是 2026-09-09 第一次真用上它的时候才写下来的

那次是 `gfex` 并进 `main`，`docs_test.go` 冲突。三句判据，一句比一句往下：

> **一、`docs_test.go` 是全生成物，它【没有独立信息】。**
> 所以它的合并结果对不对，**完全由「重造是不是幂等」决定** ——
> 不需要逐字比对两侧，也**没法**靠比对两侧来判定。
>
> **二、所以「取一侧 + 重造」不是省事，是唯一能被验证的做法。**
> 那一次的两条独立确认：`TestCarrierCensus` 过（它拿记录的数去对**实际文件**），
> 且重跑生成器后 `git status` **空**。
>
> **三、手工解一个生成物，产出的是【任何一次重造都不会复现】的东西 ——
> 而那一刻起，它就再也无法被验证了。**

**而第三句有一个能被任何人复算的例子，就在那次冲突里**（这一段是评审方要求一并写下的）：

```
                        main 侧 7058e1f   gfex 侧 f7906ed   取一侧+重造后
载体份数                      5                 5                5
CONTRIBUTING.md             237               225             **237**   ← gfex 没动它 ⇒ 取 main 侧
tools/audit/README.md        80                99              **99**   ← gfex 动了它 ⇒ 取 gfex 侧
其余三份                     三者相同
```

⇒ **两份不同的载体，各自取到了正确的那一侧** —— 不是 `--ours` 一刀切的结果。

⛔ **而这正是第三句的要害：`237` 和 `225` 摆在冲突里，两个数【都看起来合理】。**
手工解的人没有任何依据在它们之间选，**而选错了也不会有东西红** ——
因为逐行合出来的那份，从此不再是任何一次重造的输出，**再也对不上账。**

⚠️ 射程：上面三条只证明**普查表与今天的树一致**。它**不**证明两条分支的载体内容都完好 ——
但那是另一个问题：载体文件自己是干净合并的（没有冲突），它们的内容是 git 的三方合并结果，
**与 `docs_test.go` 怎么解无关。**

#### ⚠️ 这条规矩有**两个**前提，而前提比规矩本身更容易漏

##### 前提一（输出侧）：**这个文件删掉能不能一模一样地造回来**

**判据不是「这个文件看起来像生成的」，是【删掉它能不能一模一样地造回来】。**

```bash
md5sum <生成物>            # 记下这个值
rm <生成物>
<重跑生成器>
md5sum <生成物>            # 必须与上面【逐字节】相同
```

⚠️ **别用 `git status --porcelain` 代替它**：那比的是**索引里那一份**，
只在「树本来干净、且该文件当前状态已提交」时才答得对。
**而这条判据的读者最可能正在解冲突——那时树一定不干净。**

- **能** ⇒ 前提一满足。
- **不能** ⇒ 它只有一部分是生成的，**这条规矩用在它身上会静默删掉另一部分。**

##### 前提二（输入侧）：**生成器读的那些载体必须先解干净**

⚠️ **这条规矩的使用现场就是解冲突，而那时载体自己也可能带着冲突标记。**

评审方 2026-09-08 提出，我复现了：在载体里留下 `<<<<<<<` / `=======` / `>>>>>>>`，
两侧各加一条规矩，然后**照这条规矩重造**——

```
rebuild             退 0，「重建完成：锚点 49 节 / 规矩 223 条 / 普查 5 份」
两侧的规矩正文       【都进了登记表】
冲突标记本身         【没进生成物】—— 它不是加粗行，被过滤掉了
go test             【全绿】
而载体里的冲突标记    【还留着】
```

⇒ 得到一份**自洽、全绿、而把两侧内容一起烤进去**的生成物，
**且生成物上看不出任何异常**——因为标记恰好被过滤了。

> 所以上面那句要补一半：**四个绿灯检查的是「剩下的东西对不对」，
> 不是「有没有东西不见了」，也不是「有没有东西多出来」。**

###### ⚠️ 而它还会留下一个**延迟发作**的痕迹

冲突态重造会把**高水位**一起抬上去（我量到 `rules 221 → 223`），而高水位只涨不落。
于是后来有人**正确地**解掉冲突、重造：

```
红的是【解对了的那个人】，
而提示告诉他：「这张表缩水了 ⇒ 动手把 high_water.txt 那一行调低，
               并在提交信息里说删了什么」
```

**照做就把损坏烤死了。** 本仓那条「假警报比没有警报更糟」在这儿要再深一层：

> **这不只是假警报，是一条带着【错误处置建议】的假警报，
> 而它指向的恰恰是唯一做对了的那个人。**

⇒ **动手之前先扫一遍载体**，命中非零就先解载体、再动生成物：

```bash
grep -rn -e '^<<<<<<< ' -e '^=======$' -e '^>>>>>>> ' -- *.md docs tools
```

**顺序反了，上面两格都会发生**，而且第二格要等到下一个人才发作。

本仓实测过后一种：`docs_test.go` 曾经只有标记之后是生成的，
标记之前 9212 字节是**照抄盘上那一份**。于是一个**自足**的守卫可以被
「取任一侧 + 重跑生成器」静默删掉，而——

```
rebuild 退 0     gofmt 干净     vet 过     测试全绿
```

**四个绿灯，没有一个看得见那个守卫没了**：
它们检查的是「剩下的东西对不对」，**不是「有没有东西不见了」**。

⇒ 处置不是给规矩加一条例外，是**把事实改成规矩说的那样**：
把那个文件拆成「全手写」+「全生成」两份，判据才第一次成立。

#### 而这个形状不止这一个实例

```
半生成的文件         合并时不知道该合哪半
第二份拷贝（.gen）   今天一致，明天分叉
派生出来的阈值       在生成那一刻恒真
```

**三个都是「一个东西同时有两个来源」**，而本仓三个各栽过一次，
**每次都当成独立的一件事去修。**

> ⚠️ 「同一形状的第 N 次」在这儿一直是**事后的标签，不是事前的判据**。
> 写下这一格，是为了让下一次有机会反过来。

## 六、打 tag 之前

排期行的 ✅ **跟实际状态走，不跟提交走**。评审退回就改回「评审中」。

### 会在打 tag 那一刻失败的守卫，**打之前先空跑一次**

`doccheck` 的白名单「预定版本一打 tag 即 FAIL」——**打 tag 这个动作本身可能让检查变红。**
用临时本地 tag 空跑，**然后在真 tag 存在之后再跑一次**（空跑用的不是真的那一个）。

> **空跑不是为了看它退不退非零，是为了【读它说了什么】。**
> 一条只在未来才执行的分支，**它既没被执行过，也没被读过**；
> 空跑解决前者，读输出解决后者——**而只做前者会让你以为两者都解决了。**

（真事：`doccheck` 那条分支旧版新版**退出码完全一样**，差别只在那行字，
而那行字是**一句字面上的假话**，两个版本没人发现。）

### 不要提前给一个红色发通行证

「打 `v0.3.0` 那天白名单会一起到期，这是设计如此，免得有人以为守卫坏了」——
**不对。那天不该红；红了就是有一步没做**（实现完了要删 `pending.txt` 那几行）。

**提前解释一个红色，和给检查加 `|| true`，最终下场一样：没人再读它。**

## 七、合并 / 删除**之后**，回头用一条独立命令核对

改状态的命令报告的是「我发出了这个请求」，不是「现在是那个状态」。

```bash
git branch -a --format='%(refname:short) %(objectname:short)'
git ls-remote origin
```

四道处置，一次做齐（本仓「声称删了而没删」出过两次，这是复现那天挡住它的组合）：

1. **别吞输出**——不要 `>/dev/null 2>&1`；
2. **别用 `&&` 串两步**——第一步失败会让第二步根本不执行，而报告照写「已清理」；
3. **删之前**用 `git merge-base --is-ancestor` 证明内容不会丢；
4. **删之后**直接 `git ls-remote` 问远端。

> **`remotes/origin/*` 不是远端状态，是「上次 fetch 时的远端状态」。**

⚠️ 顺带一条**归因**上的：`git branch -d` 的参照物是 **upstream ref，不是 HEAD**。
它拒绝通常意味着**你有东西没推**，**与删除顺序无关**——
这条是建了个一次性仓库跑两遍才定下来的，别凭印象讲它。

## 八、没问题的时候要**明说没问题**

否则「被挑」会变成默认状态，规则就失去信息量。这是严格的另一半。

---

## 合并：生成物不合，重造；来历不取一侧，取并集

**每并完一条分支，必须跑这三步**（2026-09-09 立，双方各试并过一次）：

```bash
rm docs_test.go && python tools/audit/rebuild_docs_test.py && git diff --exit-code -- docs_test.go
```

### 三类文件，三种解法 —— 而它们【不一样】

| 文件 | 它是什么 | 怎么解冲突 |
|---|---|---|
| `docs_test.go` | **生成物** | **别手合**：删掉重跑生成器，再用上面那条断言核 |
| `tools/audit/high_water.txt` | 数值行是生成物，**来历行是日志** | 数值取两侧较大（重造会修正）；**来历行取并集、保序** |
| 其余散文/代码 | 手写 | 正常合 |

> ⛔ **`high_water.txt` 的来历行【绝不能取一侧】。**
> 两条分支各自追加过来历，取任一侧就会**丢掉另一侧的历史**——
> 而那条守卫只看「每个名字最后一行」，**丢了中间的它抓不住**（实测：删掉一条非末尾的来历行，守卫仍绿）。

⚠️ **而取并集本身也不是终点**：并集里两条来历可能都声称「从同一个数抬起」，
**合起来在讲一个没发生过的故事**。正确动作是**重造**（算出真值）＋**追加一行诚实的合流记录**。
后者要等那条链检查落地（见 `docs/design.md` §6.1 的来历那一格）；
**在它落地之前，这一步靠这份文档顶着，不靠人记得。**

### ⚠️ 那条断言必须比对的是【工作区 vs 索引】

```
对    git diff --exit-code -- docs_test.go            工作区 vs 索引
错    git diff --cached --exit-code -- docs_test.go   索引 vs HEAD —— 合并时它当然非 0
```

> **一条比错了对象的断言，比一条不存在的断言更糟**：
> 不存在的断言不会给你退出码，**比错对象的断言会给你一个【看起来像结论】的退出码。**
> ⇒ **新写一条断言，先让它在【应当失败】的那一侧跑一次。**

---

## 自检（送审前跑，输出贴进第 3 样材料）

```bash
python tools/audit/module_sweep.py && go run ./tools/doccheck
```

#### ⛔ 为什么不再是那一行 `gofmt && vet && go test ./...`

**`go test ./...` 的射程是【当前模块】，而本仓有两个：**

```
./go.mod                        根模块
./tools/probe/shinny/go.mod     天勤探针（要 coder/websocket，故意独立出去）
```

实测（2026-09-09）：第二个模块里有两条守卫，**从来没有被自检链跑过**——

```
TestProbesDoNotImportLibrary       探针不许 import 本库
                                   （拿本库去判定本库，本库错了两边会一起错）
TestGuardListCoversEveryProbeFile  豁免清单要覆盖每个探针文件
```

手动跑，它们是绿的。⚠️ **而「它们是绿的」和「它们在跑」是两件事**：

> **一条没有被任何门禁跑到的守卫，坏了不会有人知道 ——
> 它和不存在的区别，只在读者的印象里。**

⇒ `module_sweep.py` **枚举** `go.mod` 而不是写死那两个，并把找到的模块清单**打印出来**：
写死的话，第三个模块进来时没有任何机械途径提醒人把它加进去，
**那就成了又一条「靠人记得」的规矩** —— 而本仓这一类已经杀过十次。

⚠️ 它的射程也写在它自己的输出里：判据是「目录里有 `go.mod`」，
**一个不用 `go.mod` 组织的东西它照样看不见。**

```bash
python tools/probe/probe.py
```

写探针看 [`tools/probe/README.md`](tools/probe/README.md)；
改文档看 [`docs/README.md`](docs/README.md)；
规矩该往哪儿落看 [`docs/method-landing.md`](docs/method-landing.md)。
