# 仓库内一律使用 LF，并且 checkout 出来也保持 LF。
#
# 不加这个文件时，Windows 上默认的 core.autocrlf=true 会在检出时把工作区转成 CRLF，
# 于是不同平台的贡献者会因为行尾差异产生无意义的整文件 diff。这里显式钉死，
# 使行尾不再取决于各人的 git 配置。
* text=auto eol=lf

# Go 生态的文本文件全部走 LF（gofmt 输出本身就是 LF）。
*.go     text eol=lf
*.mod    text eol=lf
*.sum    text eol=lf
*.md     text eol=lf
*.yml    text eol=lf
*.yaml   text eol=lf
*.json   text eol=lf
LICENSE  text eol=lf

# ⛔ 这些声明管的是【索引】，而工作树可以和它们漂开 —— 而 `git status` 结构上看不见。
#
# 2026-09-10 实测：`LICENSE` 的工作树是 CRLF，而这里声明的是 `eol=lf`；
# 索引侧是 LF、`git status --porcelain` **0 行**。
# 成因：`text=auto` 在【入索引那一步】就归一化了 ⇒ 那个差异从来没有进过 diff。
#
#   > **一个被自动归一化掉的差异，等于一个没有观测者的差异。**
#
# ⇒ 查法（`status` 查不出来，要换一把尺）：
#
#     git ls-files --eol        # i/ 是索引侧，w/ 是工作树侧，attr/ 是这里的声明
#                               # w/ 与 attr/ 对不上的那些，就是漂开的
#
#   修法：`rm <file> && git checkout -- <file>`（按属性重新检出）。
#
# ⚠️ **而这里【不加守卫】，理由写下来**：它守的是工作树的一个装饰性属性 ——
# 提交进去的内容一直是对的（索引侧纯 LF），`gofmt` 会重写 `.go`，
# 而全仓今天**一个 `.bat`/`.cmd` 都没有**（下面那两条 `eol=crlf` 一个文件都不命中）。
# ⇒ **守卫的维护成本高过它挡住的东西。** 这一句是判断，不是遗漏；
#   哪天真有脚本文件进来、且它的行尾会影响解释器，这个判断要重做。
#
# ⚠️ 而那两条命中 0 的规则**留着**：本仓那条判据 ——
# **命中 0 的规则该留（它是射程），幽灵豁免该报（它是松动）**，两者方向相反。

# Windows 平台脚本必须是 CRLF，否则某些解释器会报错。
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做任何行尾转换。
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.gz   binary
*.zip  binary
*.pdf  binary

# 让 GitHub 的语言统计忽略示例代码。
examples/** linguist-documentation

# Python 源码走 LF。
*.py     text eol=lf
