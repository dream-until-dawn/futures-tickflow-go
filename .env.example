# 复制成 .env 后填写。.env 已在 .gitignore 里，不会进版本库。
#
# 这三个键只被【探针】用到：
#   python tools/probe/probe.py --only shinny-auth
#   cd tools/probe/shinny && go run .
# 缺任意一个，这两条探针都会 SKIP（不是 FAIL）——没有账户的人
# 克隆下来照样能跑其余 12 条新浪 / 中金所的探针。

# 快期账户。免费注册：https://account.shinnytech.com/
SHINNY_USER=
SHINNY_PASS=

# 天勤行情网关的 OIDC client_secret。
#
# 【本仓刻意不提交这个值】——它是对方 SDK 分发给所有使用者的客户端身份，
# 不是发给本项目的。contract.md 的缓解措施写着「不设默认值，必须显式提供，
# 让这成为明确的一步」，而探针也是本仓的一部分：把值提交进来，等于让
# clone 下来直接跑的人在毫无察觉的情况下用上那个身份。
#
# 取值：PyPI 的 tqsdk 包，`tqsdk/auth.py` 的 `_request_token`，
#       那里 `client_secret` 是个硬编码的 UUID。
#       pip download tqsdk --no-deps 之后解开 whl 即可看到。
#
# 详见 docs/probe.md 6.1（含它一度进入过本仓公开历史、以及历史重写
# 只做到「大幅降低」而非「彻底消除」的记录）。
SHINNY_CLIENT_SECRET=
